# Changelog

## [Unreleased]

### Added
- Plugin skeleton: config page, SQL tables, chat UI structure
- LLM provider selection (Anthropic, OpenAI, Gemini) with per-provider API key and model
- JeedomMCP connection settings (URL + API key)
- Chat UI layout (sidebar + message area + input bar)
- Mobile-responsive CSS with sidebar drawer
- PWA manifest for "Add to home screen" support
- SSE chat endpoint stub (`api/chat.php`)